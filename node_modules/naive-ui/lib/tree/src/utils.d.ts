import { Key, TreeOption } from './interface';
export declare function keysWithFilter(nodes: TreeOption[], pattern: string, filter: (pattern: string, node: TreeOption) => boolean): [Key[], Key[]];
declare const emptyImage: HTMLImageElement | null;
export { emptyImage };
